file(GLOB_RECURSE FILTERS_SOURCES CONFIGURE_DEPENDS *.cpp)
file(GLOB_RECURSE FILTERS_HEADERS CONFIGURE_DEPENDS *.h)

add_library(filters ${FILTERS_SOURCES} ${FILTERS_HEADERS})
target_include_directories(filters PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})